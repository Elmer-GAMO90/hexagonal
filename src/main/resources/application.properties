spring.application.name=hexagonal

#Conexion a la base de datos
# Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/hexagonal
spring.datasource.username=root
spring.datasource.password=password
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=true
#Ubicacion de las versiones
#spring.flyway.locations=classpath:db/migration

# JWT Configuration
app.jwt.secret=MySecretWordMySecretWordMySecretWordMySecretWord
# This is 1 hour in milliseconds (3600000ms = 60min)
app.jwt.expiration=3600000

